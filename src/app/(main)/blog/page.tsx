import { Metadata } from "next";

export const metadata : Metadata = {
  title : "Blog"
}

const Page = () => {
  return (
    <>
      <section>This is Blog Page</section>
    </>
  );
};

export default Page;
